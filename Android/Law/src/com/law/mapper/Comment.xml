<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.law.mapper.CommentMapper">
    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <resultMap type="com.law.pojo.Comment" id="commentResultMap">
    	<id column="id" property="id" jdbcType="BIGINT"/>
    	<result column="topic_id" property="topicId" jdbcType="BIGINT"/>
    	<result column="comment_content" property="commentContent" jdbcType="LONGVARCHAR"/>
    	<result column="comment_username" property="commentUserName" jdbcType="VARCHAR"/>
    	<result column="comment_time" property="commentTime" jdbcType="VARCHAR"/>
    </resultMap>
    <select id="queryById" resultMap="commentResultMap" parameterType="_long">
        <!-- 具体的sql -->
        SELECT
            *
        FROM
            comment
        WHERE
            id = #{id}
    </select>

	<select id="queryAll" resultMap="commentResultMap">
        <!-- 具体的sql -->
        SELECT
            *
        FROM
          	comment
        ORDER BY
        	id 
        DESC
    </select>
    <select id="queryByTopicId" resultMap="commentResultMap" parameterType="_long">
        <!-- 具体的sql -->
        SELECT
            *
        FROM
          	comment
        Where
         	topic_id = #{topicId}
        ORDER BY
        	id 
        DESC
    </select>
	<insert id="insert"  parameterType="Comment">
		INSERT ignore INTO comment(topic_id,comment_content,comment_username,comment_time)
		VALUES(#{topicId},#{commentContent},#{commentUserName},#{commentTime})
	</insert>

    <update id="update" parameterType="Comment">
        UPDATE comment
        SET 
        	topic_id = #{topicId}
         	comment_content = #{commentContent},
        	comment_username = #{commentUserName},
        	comment_time = #{commentTime}
        WHERE
            id = #{id}
        AND number > 0
    </update>
    
    <delete id="delete" parameterType="_long">
       DELETE 
       FROM comment
       WHERE id = #{id}
    </delete>
</mapper>